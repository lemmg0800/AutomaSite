- **Tailwind CSS** — utility-first framework supplying every layout, color, spacing and effect utility (loaded via `assets/resource_3fa48481346f.js`)
- **Iconify (Solar icon set)** — icon system rendering all `data-icon="solar:..."` SVGs and `<span class="iconify">` placeholders (`assets/iconify_654a1ef798a3.js`)
- **Chart.js** — drives the bar chart inside the hero "Sucesso de entregas" bento card (`assets/chart_ba9eca532d10.js`)
- **UnicornStudio.js** — third-party WebGL runtime injected at runtime to render the animated aura background (`.aura-background-component`)
- **Inter (Google Fonts)** — primary UI typeface, served locally from the bundled `UcC73FwrK3iLTeHuS_*.woff2` files via `assets/css2_2832c0ff631d.css`
- **IntersectionObserver API** — used in `assets/js/scroll-reveal.js` to play the `fadeSlideIn` animation when each `.animate-on-scroll` element enters the viewport
- **Custom CSS** — `fadeSlideIn` keyframe and `.border-gradient` masked gradient border utility live in `assets/css/animations.css`; defensive scroll/visibility resets live in `assets/css/scroll-fallback.css`
</content>
</invoke>